payments.db.host=localhost

# Payments R2DBC Connection
spring.r2dbc.url=r2dbc:postgresql://${payments.db.host}:5432/payments

# Flyway properties
spring.flyway.url=jdbc:postgresql://${payments.db.host}:5432/payments
spring.flyway.schemas=event_service
spring.flyway.baseline-on-migrate=true
