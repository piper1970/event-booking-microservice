encrypt.key=${CONFIG_SERVER_KEY}
eureka.client.service-url.defaultZone=http://${DISCOVERY_USER}:${DISCOVERY_PASSWORD}@localhost:8089/eureka

spring.cloud.config.fail-fast=true
spring.cloud.config.username=${CONFIG_USERNAME}
spring.cloud.config.password=${CONFIG_PASSWORD}