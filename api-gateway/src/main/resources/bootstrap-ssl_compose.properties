# all tracing metrics from zipkin docker address
management.zipkin.tracing.endpoint=http://zipkin:9411/api/v2/spans

# setup spring-cloud configuration using internal docker address
spring.cloud.config.uri=http://event-service-config:8888
spring.cloud.config.fail-fast=false

# enabled discovery client, connecting to internal docker discovery-server address
eureka.client.service-url.defaultZone=http://${DISCOVERY_USER}:${DISCOVERY_PASSWORD}@discovery-server:8089/eureka
eureka.client.enabled=true
